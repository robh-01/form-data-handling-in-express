<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    <% if (locals.searchEmail) { %>
      <h2>Search results for: <%= searchEmail %></h2>
      <% if (locals.user) { %>
        <p>User found:</p>
        <p>Name: <%= user.firstName %> <%= user.lastName %></p>
        <p>Email: <%= user.email %></p>
      <% } else { %>
        <p>No user found with that email.</p>
      <% } %>
    <% } %>
    <a href="/">Back to home</a>
  </body>
</html>